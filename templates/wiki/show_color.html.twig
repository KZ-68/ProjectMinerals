{% extends 'base.html.twig' %}

{% block title %}Color Details{% endblock %}

{% block body %}

<h1>Minerals list for <strong>{{color.name}}</strong></h1>
<hr>

<div class="list-wrapper">
    <div>
        <ul>
            {% if color.minerals | length > 0 %}
                {% for mineral in color.minerals %}
                <li><a href="{{ path('show_mineral', {'slug': mineral.slug}) }}">{{mineral.name}}</a></li>
                {% endfor %}
            {% else %}
                <p>There's no mineral added yet</p>
            {% endif %}
        </ul>
    </div>
</div>

{% endblock %}