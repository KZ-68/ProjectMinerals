{% extends 'base.html.twig' %} 

{% block title %}Ajouter une session{% endblock %}

{% block body %}
    {# {% if sessionId %}
    <h1>Editer une session</h1>
    {% else %}
    <h1>Ajouter une session</h1>
    {% endif %} #}
<div class="formulaire">
    {{ form_start(form) }}
        {{ form_row(form.name)}}
        {{ form_row(form.formula)}}
        {{ form_row(form.crystal_system)}}
        {{ form_row(form.density)}}
        {{ form_row(form.hardness)}}
        {{ form_row(form.fracture)}}
        {{ form_row(form.streak)}}
        {{ form_row(form.category)}}
        {{ form_row(form.colors)}}
        {{ form_row(form.lustres)}}
        {# <ul class="colors"
            data-index="{{ form.colors|length > 0 ? form.colors|last.vars.name + 1 : 0 }}"
            data-prototype="{{ form_widget(form.colors.vars.prototype)|e('html_attr') }}">
            {% for color in form.colors %}
                {{ form_errors(color) }}
                {{ form_widget(color.name, {'attr': {'class': 'borders'}}) }}
            {% endfor %}
        </ul> 
        <button type="button" class="add_item_link" data-collection-holder-class="colors">Add a color</button> #}
        {{ form_row(form.submit)}}
    {{ form_end(form) }}
</div>

<script src="/js/collection.js">
</script>

{% endblock %}