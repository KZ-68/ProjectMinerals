{% extends 'base.html.twig' %} 

{% block title %}Edit role user{% endblock %}

{% block body %}

<div id="edit-role-user-wrapper">
    <aside class="aside-wrapper">
        {% include "_partials/_admin_nav.html.twig"%}
    </aside>

    <div aria-labelledby="edit-role-user-heading" id="edit-role-user-right">

        <h1 id="edit-role-user-heading">Edit role-user</h1>
        <hr>

        <div class="formulaire-wrapper-role">
            {{ form_start(editUserForm) }}
            <div class="formulaire">
                {{ form_row(editUserForm.user) }}
                {{ form_row(editUserForm.roles)}}
                {{ form_row(editUserForm.save)}}
            </div>
            {{ form_end(editUserForm) }}
        </div>
    </div>
</div>

{% endblock %}